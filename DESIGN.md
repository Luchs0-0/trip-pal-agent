# TripPal · 设计文档 (Design Doc)

> 香港 / 内地两地行程助手：一个用自然语言查询两地节假日与行程安排的 LangGraph Agent。
> 本文档记录**为什么这么设计**，让读 repo 的人（包括未来的你）理解设计意图。

---

## 1. 项目定位

**一句话**：用户用自然语言提问（如"2026 年国庆放哪几天？""内地和香港下个共同长假期是什么时候？"），
Agent 自主判断需要哪些工具、分步调用、最后给出自然语言回答。

**价值**：香港与内地的公众假期体系不同（节日、日期、调休规则各异），
人工对照两地假期表很麻烦。这个 Agent 让"查两地假期"变成一个自然语言对话。

**个人目标**（对作者）：
- 亲手实现一个**工具调用循环（tool-calling loop）**的 Agent，理解其核心机制；
- 用 LangGraph 做编排，贴合 AI 交付（FDE）方向；
- 做成**工程完整、可上 GitHub、可录 demo** 的个人作品。

---

## 2. 核心概念：工具调用循环（Tool-Calling Loop）

这是本项目最重要的心智模型。一个最小 Agent 的本质是：

```
        ┌─────────────────────────────┐
        │           LLM（模型）          │
        │   "根据上下文，下一步做什么？"   │
        └──────────────┬──────────────┘
                       │
         有工具调用需求   │            无（可以回答）
        ┌───────────────┴───────────────┐
        ▼                               ▼
┌─────────────────┐              输出最终答案
│  执行工具 (Tool)  │
│  模型决定调用哪个  │
│  传什么参数        │
└────────┬────────┘
         │ 工具返回结构化结果
         ▼
  （结果追加回上下文，回到循环顶部）
```

**关键点**：
- 模型**每次只输出"下一步动作"或"最终答案"**，不是一次吐完；
- 框架（LangGraph）负责：读模型输出 → 若要求调工具 → 执行并回填 → 再让模型继续；
- 循环终止条件：模型认为可以回答（不再要求工具）。

---

## 3. 技术选型与理由

| 组件 | 选择 | 理由 |
|---|---|---|
| Agent 编排 | **LangGraph** | 状态图清晰、可扩展、贴合 FDE 方向；把循环变成可编排/可调试的结构 |
| LLM | **DeepSeek（OpenAI 兼容）** | 便宜、中文好、用 `base_url` 兼容 OpenAI SDK |
| 数据 | **本地 JSON**（一次性抓取转存） | 稳定、离线可测、数据与代码分离；不依赖第三方 API |
| Web UI | **轻量方案（后端 + 单页）** | 需求 A：极简可用、保留扩展性 |
| 工具 | 4 个结构化工具 | 小、明确、能体现多步调用 |

---

## 4. 工具集设计

| 工具 | 功能 | 示例问题 |
|---|---|---|
| `get_holidays_cn(year, month?)` | 查内地节假日 + 调休安排 | "2026 年国庆放哪几天？" |
| `get_holidays_hk(year, month?)` | 查香港公众假期 | "2026 年香港复活节是哪天？" |
| `find_common_breaks(year)` | 求两地共同/接近重叠的假期窗口 | "内地和香港下个共同长假期？" |
| `days_until(date)` | 距离某日期还有几天 / 两日期差 | "距离下个国庆还有几天？" |

**设计原则**：
- 工具**自描述**：函数名 + docstring + 类型注解 → 转成模型能读的 schema；
- 工具**边界安全**：只做查询，不做写操作；
- 工具**可组合**：`find_common_breaks` 内部可复用 `get_holidays_cn/hk` 的数据，体现"多步决策"。

---

## 5. 数据设计

### 数据源（官方权威）
- **香港**：GovHK 各年公众假期页（2025 / 2026 / 2027）
  `https://www.gov.hk/tc/about/abouthk/holiday/2027.htm`
- **内地**：国务院办公厅放假安排通知（2025、2026；2027 待官方发布）
  `https://www.gov.cn/zhengce/content/202511/content_7047090.htm`

### 数据获取方式
1. 一次性抓取官方页面 → 解析 → 生成 `data/holidays_*.json`；
2. 运行时**只读本地 JSON**，不依赖网络（稳定、可测）；
3. README 注明"如何日后替换为实时源"。

### 数据格式（示例）
```json
{
  "year": 2027,
  "region": "hk",
  "holidays": [
    {"name": "一月一日", "date": "2027-01-01", "weekday": "星期五"},
    {"name": "農曆年初一", "date": "2027-02-06", "weekday": "星期六"}
  ]
}
```

---

## 6. 系统架构（目录规划）

```
trip-pal/
├── DESIGN.md               # 本文档
├── README.md               # 使用说明（面向用户/GitHub）
├── requirements.txt
├── .gitignore
├── data/                   # JSON 数据（抓取后转存，随 repo 提交）
│   ├── holidays_hk_2025.json
│   ├── holidays_hk_2026.json
│   ├── holidays_hk_2027.json
│   └── holidays_cn_2025.json, holidays_cn_2026.json
├── scripts/
│   └── fetch_data.py       # 抓取官方页面 → 生成 JSON（一次性）
├── src/trip_pal/
│   ├── __init__.py
│   ├── config.py           # 模型/路径配置
│   ├── data_loader.py      # 读 JSON → 提供工具用数据
│   ├── tools.py            # 4 个工具（自描述）
│   ├── graph.py            # LangGraph 编排（Agent 循环）
│   └── agent.py            # 对外封装：给一句话 → 返回回答
├── web/
│   ├── app.py              # Web 后端（FastAPI/Flask）
│   └── static/
│       └── index.html      # 单页 UI（极简，含工具调用过程展示）
└── tests/                  # （可选）工具层单元测试
```

---

## 7. Web UI 设计（需求 A：极简 + 可扩展）

- **后端**：一个轻量 Web 服务，暴露 `POST /ask`（接收问题 → 调 agent → 返回回答 + 工具调用轨迹）；
- **前端**：单页 HTML，聊天式界面；**显示 agent 的思考过程**（调用工具名、参数、返回）——这是 demo 亮点；
- **扩展性**：预留"会话历史、工具开关、更好样式"的接口，但不阻塞当前极简版。

---

## 8. 验收标准（DoD）

- [ ] `python scripts/fetch_data.py` 能抓取并生成两地 JSON 数据（离线可用）
- [ ] `python -m trip_pal.cli` 能命令行问答（可选入口）
- [ ] `python web/app.py` 启动 Web UI，输入问题能拿到回答 + 工具调用轨迹
- [ ] 至少演示 3 类问题：查内地、查香港、查共同假期
- [ ] README 完整（简介/安装/运行/架构图/数据来源/未来扩展）
- [ ] 推上 GitHub，别人 clone 后按 README 能跑起来

---

## 9. 未来扩展（明确不做 / 保留接口）

- 实时数据源（政府 API / 第三方）——预留 `data_loader` 接口
- 多步复杂规划（行程规划、跨城市路线）——LangGraph 支持，后续可加 node
- 会话记忆 / 多轮上下文
- Web UI 美化 / 部署上线

---

*设计文档 v1.0 · 2026-08-14*
